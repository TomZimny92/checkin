<?xml version="1.0" encoding="utf-8" ?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Checkin"
             xmlns:vm="clr-namespace:Checkin.ViewModels"
             x:Class="Checkin.MainPage"
             Title="Checkin">

    <ContentPage.BindingContext>
        <vm:CheckinViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *, Auto" BackgroundColor="FloralWhite">
        <Border Grid.Row="1"
                Margin="10"
                Stroke="LightBlue"
                StrokeThickness="1"
                BackgroundColor="Beige"
                Padding="20"
                VerticalOptions="Fill">
            <VerticalStackLayout Spacing="20" HorizontalOptions="Center" VerticalOptions="Center">
                <Label
                    x:Name="ClockDisplay"
                    Style="{StaticResource Headline}"
                    SemanticProperties.HeadingLevel="Level1" />

                <Button Text="Check IN"
                        Command="{Binding CheckinButtonCommand}"
                        IsEnabled="{Binding !IsCheckedIn}"
                        WidthRequest="200"
                        HeightRequest="50"
                        BackgroundColor="Green"
                        TextColor="White" />

                <Button Text="Check OUT"
                        Command="{Binding CheckoutButtonCommand}"
                        IsEnabled="{Binding IsCheckedIn}"
                        WidthRequest="200"
                        HeightRequest="50"
                        BackgroundColor="Red"
                        TextColor="White" />
                
            </VerticalStackLayout>
        </Border>
        <ScrollView Orientation="Horizontal" Grid.Row="0" Margin="0,5">

        </ScrollView>
    </Grid>
    <!--<ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                x:Name="ClockDisplay"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            
            <Label
                x:Name="TestLabel"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Button
                x:Name="CheckinButton"
                Text="Checkin"
                TextColor="White"
                FontSize="Header"
                BackgroundColor="Green"
                Clicked="Checkin"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                IsEnabled="{Binding IsCheckedIn}"/>

            <Button
                x:Name="CheckoutButton"
                Text="Checkout" 
                TextColor="White"
                FontSize="Header"
                BackgroundColor="Red"
                Clicked="Checkout"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                IsEnabled="{Binding CheckedIn}"/>

            <Button 
                Text="Summary"
                TextColor="White"
                FontSize="Header"
                BackgroundColor="Blue"
                Clicked="GetSummary"
                HorizontalOptions="Center"
                VerticalOptions="Center" />

            <Button 
                Text="Reset"
                TextColor="White"
                FontSize="Medium"
                BackgroundColor="Gray"
                Clicked="GetSummary"
                HorizontalOptions="Center"
                VerticalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>-->

</ContentPage>
